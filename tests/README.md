# Installation
Create a python virtual environment in `BitCrusher/tests` and install the BitCrusher module relatively
```
python -m venv ./.venv && source .venv/bin/activate
pip install -r requirements.txt
pip install -e ../interface/
```